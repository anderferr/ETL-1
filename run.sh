docker compose build
docker compose up

# pylint app.py --disable=import-error
# pip freeze > requirements.txt
# docker compose up --remove-orphans
# docker compose run server pylint app.py
# docker compose run bash
