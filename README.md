# ETL from webscraping

The initial idea is to develope an ETL pipeline to manage the extraction of data via webscraping.

The project is in a docker container, using pylint as linter and pytest as a tester.
